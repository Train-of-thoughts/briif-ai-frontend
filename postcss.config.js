/** @type {import('postcss-load-config').Config} */
module.exports = {
    plugins: {
        // новий офіційний плагін для Tailwind v4
        '@tailwindcss/postcss': {},
        autoprefixer: {},
    },
};
